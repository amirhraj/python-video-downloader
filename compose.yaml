services:
  downloader:
    build:
      context: .
      dockerfile: Dockerfile

    image: python-video-downloader:latest

    volumes:
      - ./downloads:/app/downloads

    working_dir: /app

    restart: "no"